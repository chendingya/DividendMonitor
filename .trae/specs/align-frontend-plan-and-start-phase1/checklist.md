- [x] `FRONTEND-IMPLEMENTATION-PLAN.md` 已与 PRD/SDD 的能力范围对齐，并补齐可执行验收点
- [x] 一级导航、顶部栏、关键卡片图标已统一语义与样式
- [x] 核心跳转链路可走通：概览 -> 详情、自选 -> 详情、自选 -> 对比 -> 详情、详情 -> 回测
- [x] 路由或页面上下文可承载股票标识，避免只依赖硬编码默认值
- [x] `npm run typecheck` 通过且无新增诊断错误
- [x] 主要页面状态（loading/error/empty）在首轮改造后未退化
