# 股息率地图（yield-map）实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现股息率地图页 `/yield-map`：全市场 A 股（约 5,888 只）TTM 股息率按行业 Treemap 可视化，支持行业/股票 drill-down、颜色映射、点击跳转详情与对比，本地 SQLite 快照 + Supabase 行业级云端同步。

**Architecture:** 调研见 `docs/yield-map-research.md`（方案 A 已确认）。数据源：clist 全市场行情+行业（59 页）+ RPT_SHAREBONUS_DET 全市场分红事件（113 页），走既有 Endpoint/SourceGateway 模式；TTM 口径在 domain 纯函数聚合；本地 `yield_map_snapshots` 快照表；在线模式行业级快照上传 Supabase `industry_yield_snapshots`。前端 ECharts treemap。

**Tech Stack:** TypeScript 5.8 strict / node:sqlite / Vitest 4（node 环境）/ Electron IPC / React 18 + antd 5 + ECharts 5 / Supabase。

**Spec:** `docs/yield-map-research.md`

## Global Constraints

- 严格分层单向依赖：`domain → application → repositories/adapters → infrastructure`，禁止反向依赖
- 领域层 `src/main/domain/` 不依赖 Electron、React、`node:sqlite`、任何 IO API
- 路径别名：`@main/*`、`@preload/*`、`@renderer/*`、`@shared/*`
- 渲染进程不新增测试基础设施（无 jsdom/@testing-library），前端靠 `npm run typecheck` + MCP 端到端验收
- DB schema 变更走幂等迁移：新建 `migrate*` 函数 → `initializeSchema` 追加调用 → `tests/main/infrastructure/dbMigration.test.ts` 补 `:memory:` 测试
- 股息率口径：**TTM** = 近 12 个月（exDate 在 `[today-365d, today]`）`∑(PRETAX_BONUS_RMB/10)` ÷ 最新价；无分红或无效价格 → `yieldTtm = 0` 且不入行业聚合
- 抓取失败单页跳过（partial 降级），结果标注 `partial: true`，不阻断主流程
- 在线模式行业快照按 `user_id` 隔离（RLS），股票级数据仅存本地
- 提交信息遵循 conventional commits（中文），不加 Co-Authored-By
- 每个任务完成后 `npm run typecheck` + 相关测试通过才提交

---

# Phase A：领域层聚合纯函数

## Task A1: yieldMap 领域类型与 buildYieldMap 纯函数

**Files:**
- Create: `src/main/domain/services/yieldMapService.ts`
- Test: `tests/main/domain/services/yieldMapService.test.ts`

**Interfaces:**
- Produces（后续任务依赖）：
  - `export type YieldMapQuote = { code: string; market: string; name: string; price?: number; industry?: string }`
  - `export type YieldMapDividendEvent = { code: string; exDate?: string; pretaxBonusRmb?: number }`
  - `export type YieldMapStockEntry = { assetKey: string; symbol: string; name: string; industry: string; price?: number; yieldTtm: number; totalDps12m?: number }`
  - `export function buildYieldMap(quotes: YieldMapQuote[], events: YieldMapDividendEvent[], today?: Date): YieldMapStockEntry[]`

- [ ] **Step 1: 写失败测试**

创建 `tests/main/domain/services/yieldMapService.test.ts`：

```ts
import { describe, expect, it } from 'vitest'
import { buildYieldMap } from '@main/domain/services/yieldMapService'

const TODAY = new Date('2026-08-03T00:00:00.000Z')

describe('buildYieldMap', () => {
  it('按 TTM 口径聚合：近12个月∑每股派息 ÷ 最新价', () => {
    const quotes = [{ code: '600519', market: '1', name: '贵州茅台', price: 1450, industry: '白酒' }]
    const events = [
      { code: '600519', exDate: '2026-06-20', pretaxBonusRmb: 300 },  // 每股 30 元
      { code: '600519', exDate: '2025-06-20', pretaxBonusRmb: 300 }   // 超 12 个月，不计
    ]
    const result = buildYieldMap(quotes, events, TODAY)
    expect(result).toHaveLength(1)
    expect(result[0].yieldTtm).toBeCloseTo(30 / 1450, 6)
    expect(result[0].totalDps12m).toBeCloseTo(30, 6)
    expect(result[0].assetKey).toBe('STOCK:A_SHARE:600519')
    expect(result[0].industry).toBe('白酒')
  })

  it('无分红事件 → yieldTtm = 0', () => {
    const quotes = [{ code: '000001', market: '0', name: '平安银行', price: 10, industry: '银行' }]
    const result = buildYieldMap(quotes, [], TODAY)
    expect(result[0].yieldTtm).toBe(0)
    expect(result[0].totalDps12m).toBeUndefined()
  })

  it('价格缺失或非正 → yieldTtm = 0', () => {
    const quotes = [
      { code: '000001', market: '0', name: '停牌股', industry: '银行' },
      { code: '000002', market: '0', name: '零价股', price: 0, industry: '地产' }
    ]
    const events = [{ code: '000001', exDate: '2026-05-01', pretaxBonusRmb: 10 }]
    const result = buildYieldMap(quotes, events, TODAY)
    expect(result[0].yieldTtm).toBe(0)
    expect(result[1].yieldTtm).toBe(0)
  })

  it('无行业 → 归入「未分类」', () => {
    const quotes = [{ code: '000001', market: '0', name: '无名股', price: 10 }]
    const result = buildYieldMap(quotes, [], TODAY)
    expect(result[0].industry).toBe('未分类')
  })

  it('同股票一年两次分红自动累加', () => {
    const quotes = [{ code: '601398', market: '1', name: '工商银行', price: 5, industry: '银行' }]
    const events = [
      { code: '601398', exDate: '2026-07-10', pretaxBonusRmb: 1.4 },
      { code: '601398', exDate: '2026-01-15', pretaxBonusRmb: 1.6 }
    ]
    const result = buildYieldMap(quotes, events, TODAY)
    expect(result[0].yieldTtm).toBeCloseTo(0.3 / 5, 6)
  })

  it('exDate 缺失的事件不参与 TTM 累加', () => {
    const quotes = [{ code: '600000', market: '1', name: '浦发银行', price: 8, industry: '银行' }]
    const events = [{ code: '600000', pretaxBonusRmb: 4 }]
    const result = buildYieldMap(quotes, events, TODAY)
    expect(result[0].yieldTtm).toBe(0)
  })
})
```

- [ ] **Step 2: 运行测试确认失败**

Run: `npx vitest run tests/main/domain/services/yieldMapService.test.ts`
Expected: FAIL（模块不存在）

- [ ] **Step 3: 实现 buildYieldMap**

创建 `src/main/domain/services/yieldMapService.ts`：

```ts
export type YieldMapQuote = {
  code: string
  market: string
  name: string
  price?: number
  industry?: string
}

export type YieldMapDividendEvent = {
  code: string
  exDate?: string
  pretaxBonusRmb?: number
}

export type YieldMapStockEntry = {
  assetKey: string
  symbol: string
  name: string
  industry: string
  price?: number
  yieldTtm: number
  totalDps12m?: number
}

const YIELD_MAP_LOOKBACK_DAYS = 365

function isWithinLookback(exDate: string | undefined, today: Date): boolean {
  if (!exDate || !/^\d{4}-\d{2}-\d{2}$/.test(exDate)) return false
  const [year, month, day] = exDate.split('-').map(Number)
  if (!year || !month || !day) return false
  const date = new Date(Date.UTC(year, month - 1, day))
  if (Number.isNaN(date.getTime())) return false
  const lowerBound = today.getTime() - YIELD_MAP_LOOKBACK_DAYS * 24 * 60 * 60 * 1000
  return date.getTime() >= lowerBound && date.getTime() <= today.getTime()
}

export function buildYieldMap(
  quotes: YieldMapQuote[],
  events: YieldMapDividendEvent[],
  today: Date = new Date()
): YieldMapStockEntry[] {
  const dpsByCode = new Map<string, number>()
  for (const event of events) {
    if (!isWithinLookback(event.exDate, today)) continue
    const dps = (event.pretaxBonusRmb ?? 0) / 10
    if (dps <= 0) continue
    dpsByCode.set(event.code, (dpsByCode.get(event.code) ?? 0) + dps)
  }

  return quotes.map((quote) => {
    const price = quote.price != null && Number.isFinite(quote.price) && quote.price > 0 ? quote.price : undefined
    const totalDps12m = dpsByCode.get(quote.code)
    const yieldTtm = price && totalDps12m ? totalDps12m / price : 0
    const symbol = quote.code
    const entry: YieldMapStockEntry = {
      assetKey: `STOCK:A_SHARE:${symbol}`,
      symbol,
      name: quote.name,
      industry: quote.industry?.trim() || '未分类',
      price,
      yieldTtm,
      totalDps12m
    }
    return entry
  })
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `npx vitest run tests/main/domain/services/yieldMapService.test.ts`
Expected: PASS（5 个用例）

- [ ] **Step 5: typecheck + 提交**

Run: `npm run typecheck`
```bash
git add src/main/domain/services/yieldMapService.ts tests/main/domain/services/yieldMapService.test.ts
git commit -m "feat(yield-map): 领域层 TTM 股息率聚合纯函数"
```

## Task A2: buildIndustryYieldMap 行业聚合纯函数

**Files:**
- Modify: `src/main/domain/services/yieldMapService.ts`
- Test: `tests/main/domain/services/yieldMapService.test.ts`

**Interfaces:**
- Produces：`export type YieldMapIndustryEntry = { industry: string; medianYield: number; avgYield: number; stockCount: number }`
  - `export function buildIndustryYieldMap(stocks: YieldMapStockEntry[]): YieldMapIndustryEntry[]`
  - 只统计 `yieldTtm > 0` 的股票；`medianYield` 为中位数（偶数个取中间两值平均）；结果按 `medianYield` 降序

- [ ] **Step 1: 追加失败测试**

在 `tests/main/domain/services/yieldMapService.test.ts` 追加：

```ts
import { buildIndustryYieldMap, buildYieldMap } from '@main/domain/services/yieldMapService'

describe('buildIndustryYieldMap', () => {
  it('按行业分组，中位数/均值/样本数正确且降序', () => {
    const stocks = [
      { assetKey: 'a', symbol: 'a', name: 'a', industry: '银行', price: 1, yieldTtm: 0.05 },
      { assetKey: 'b', symbol: 'b', name: 'b', industry: '银行', price: 1, yieldTtm: 0.07 },
      { assetKey: 'c', symbol: 'c', name: 'c', industry: '白酒', price: 1, yieldTtm: 0.03 },
      { assetKey: 'd', symbol: 'd', name: 'd', industry: '银行', price: 1, yieldTtm: 0.09 },
      { assetKey: 'e', symbol: 'e', name: 'e', industry: '银行', price: 1, yieldTtm: 0.11 }
    ]
    const result = buildIndustryYieldMap(stocks as never)
    expect(result).toHaveLength(2)
    expect(result[0].industry).toBe('银行')
    expect(result[0].medianYield).toBeCloseTo(0.07, 6)
    expect(result[0].avgYield).toBeCloseTo(0.08, 6)
    expect(result[0].stockCount).toBe(4)
    expect(result[1].industry).toBe('白酒')
  })

  it('yieldTtm=0 的股票不进入行业聚合', () => {
    const stocks = [
      { assetKey: 'a', symbol: 'a', name: 'a', industry: '银行', price: 1, yieldTtm: 0 },
      { assetKey: 'b', symbol: 'b', name: 'b', industry: '银行', price: 1, yieldTtm: 0.06 }
    ]
    const result = buildIndustryYieldMap(stocks as never)
    expect(result).toHaveLength(1)
    expect(result[0].stockCount).toBe(1)
    expect(result[0].medianYield).toBeCloseTo(0.06, 6)
  })

  it('空输入返回空数组', () => {
    expect(buildIndustryYieldMap([])).toEqual([])
  })
})
```

- [ ] **Step 2: 运行测试确认失败**

Run: `npx vitest run tests/main/domain/services/yieldMapService.test.ts`
Expected: FAIL（buildIndustryYieldMap 不存在）

- [ ] **Step 3: 实现 buildIndustryYieldMap**

在 `src/main/domain/services/yieldMapService.ts` 追加：

```ts
export type YieldMapIndustryEntry = {
  industry: string
  medianYield: number
  avgYield: number
  stockCount: number
}

function median(values: number[]): number {
  if (values.length === 0) return 0
  const sorted = [...values].sort((a, b) => a - b)
  const mid = Math.floor(sorted.length / 2)
  return sorted.length % 2 === 0
    ? (sorted[mid - 1] + sorted[mid]) / 2
    : sorted[mid]
}

export function buildIndustryYieldMap(stocks: YieldMapStockEntry[]): YieldMapIndustryEntry[] {
  const groups = new Map<string, number[]>()
  for (const stock of stocks) {
    if (stock.yieldTtm <= 0) continue
    const list = groups.get(stock.industry) ?? []
    list.push(stock.yieldTtm)
    groups.set(stock.industry, list)
  }

  return [...groups.entries()]
    .map(([industry, yields]) => ({
      industry,
      medianYield: median(yields),
      avgYield: yields.reduce((sum, value) => sum + value, 0) / yields.length,
      stockCount: yields.length
    }))
    .sort((a, b) => b.medianYield - a.medianYield)
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `npx vitest run tests/main/domain/services/yieldMapService.test.ts`
Expected: PASS（8 个用例）

- [ ] **Step 5: typecheck + 提交**

Run: `npm run typecheck`
```bash
git add src/main/domain/services/yieldMapService.ts tests/main/domain/services/yieldMapService.test.ts
git commit -m "feat(yield-map): 行业级股息率聚合（中位数/均值/样本数）"
```

---

# Phase B：数据源 endpoint 与适配器

## Task B1: sourceTypes 增加 market.clist / market.dividend capability

**Files:**
- Modify: `src/main/infrastructure/dataSources/types/sourceTypes.ts:6-17`

**Interfaces:**
- Produces（Task B2/B4 依赖）：
  - `Capability` 增加 `'market.clist'`、`'market.dividend'`
  - `export type MarketQuoteRecord = { code: string; market: string; name: string; price?: number; industry?: string }`
  - `export type MarketClistInput = { page: number; pageSize?: number }`
  - `export type MarketClistOutput = { records: MarketQuoteRecord[]; total: number }`
  - `export type MarketDividendRecord = { code: string; name?: string; exDate?: string; pretaxBonusRmb?: number; assignProgress?: string }`
  - `export type MarketDividendInput = { page: number; pageSize?: number }`
  - `export type MarketDividendOutput = { records: MarketDividendRecord[]; total: number }`

- [ ] **Step 1: 修改 Capability 联合类型与新增类型**

在 `src/main/infrastructure/dataSources/types/sourceTypes.ts`：

```ts
export type Capability =
  | 'asset.search'
  | 'asset.profile'
  | 'asset.quote'
  | 'asset.dividend'
  | 'asset.kline'
  | 'valuation.snapshot'
  | 'valuation.percentile'
  | 'valuation.trend'
  | 'benchmark.kline'
  | 'fx.quote'
  | 'housing.priceIndex'
  | 'market.clist'
  | 'market.dividend'
```

在文件末尾（`HousingPriceIndexOutput` 之后）追加：

```ts
// ====== Market-wide yield map capability types ======

export type MarketQuoteRecord = {
  code: string
  market: string
  name: string
  price?: number
  industry?: string
}

export type MarketClistInput = {
  page: number
  pageSize?: number
}

export type MarketClistOutput = {
  records: MarketQuoteRecord[]
  total: number
}

export type MarketDividendRecord = {
  code: string
  name?: string
  exDate?: string
  pretaxBonusRmb?: number
  assignProgress?: string
}

export type MarketDividendInput = {
  page: number
  pageSize?: number
}

export type MarketDividendOutput = {
  records: MarketDividendRecord[]
  total: number
}
```

- [ ] **Step 2: typecheck**

Run: `npm run typecheck`
Expected: PASS（新增类型无使用点，不会报错）

- [ ] **Step 3: 提交**

```bash
git add src/main/infrastructure/dataSources/types/sourceTypes.ts
git commit -m "feat(yield-map): 新增 market.clist / market.dividend capability 类型"
```

## Task B2: eastmoneyEndpoints 分页 endpoint（clist + RPT_SHAREBONUS_DET）

**Files:**
- Modify: `src/main/infrastructure/dataSources/registry/eastmoneyEndpoints.ts`
- Test: `tests/main/dataSources/eastmoneyMarketEndpoints.test.ts`

**Interfaces:**
- Produces：
  - `export const eastmoneyMarketClistEndpoint: EndpointDefinition<MarketClistInput, EastmoneyClistResponse, MarketClistOutput>`
  - `export const eastmoneyMarketDividendEndpoint: EndpointDefinition<MarketDividendInput, EastmoneyDividendResponse, MarketDividendOutput>`

- [ ] **Step 1: 写失败测试（构造返回结构断言，不发网络请求）**

创建 `tests/main/dataSources/eastmoneyMarketEndpoints.test.ts`：

```ts
import { describe, expect, it } from 'vitest'
import {
  eastmoneyMarketClistEndpoint,
  eastmoneyMarketDividendEndpoint
} from '@main/infrastructure/dataSources/registry/eastmoneyEndpoints'

describe('eastmoneyMarketClistEndpoint', () => {
  it('buildUrl 分页参数正确且带行业字段', () => {
    const url = eastmoneyMarketClistEndpoint.buildUrl({ page: 3, pageSize: 100 })
    expect(url).toContain('pn=3')
    expect(url).toContain('pz=100')
    expect(url).toContain('fields=f12,f13,f14,f2,f100')
  })

  it('mapResponse 映射代码/名称/价格/行业并透传 total', () => {
    const raw = {
      data: {
        total: 5888,
        diff: [
          { f12: '600519', f13: 1, f14: '贵州茅台', f2: 1450.0, f100: '白酒' },
          { f12: '920258', f13: 0, f14: '新股', f2: '-', f100: null }
        ]
      }
    }
    const result = eastmoneyMarketClistEndpoint.mapResponse(raw as never, { page: 1 })
    expect(result.total).toBe(5888)
    expect(result.records).toHaveLength(2)
    expect(result.records[0]).toEqual({
      code: '600519',
      market: '1',
      name: '贵州茅台',
      price: 1450,
      industry: '白酒'
    })
    expect(result.records[1].price).toBeUndefined()
    expect(result.records[1].industry).toBeUndefined()
  })
})

describe('eastmoneyMarketDividendEndpoint', () => {
  it('buildUrl 分页参数正确', () => {
    const url = eastmoneyMarketDividendEndpoint.buildUrl({ page: 2, pageSize: 500 })
    expect(url).toContain('pageNumber=2')
    expect(url).toContain('pageSize=500')
    expect(url).toContain('reportName=RPT_SHAREBONUS_DET')
  })

  it('mapResponse 映射分红事件并透传 total', () => {
    const raw = {
      result: {
        count: 56378,
        data: [
          {
            SECURITY_CODE: '600519',
            SECURITY_NAME_ABBR: '贵州茅台',
            EX_DIVIDEND_DATE: '2026-06-20 00:00:00',
            PRETAX_BONUS_RMB: 300,
            ASSIGN_PROGRESS: '实施'
          }
        ]
      }
    }
    const result = eastmoneyMarketDividendEndpoint.mapResponse(raw as never, { page: 1 })
    expect(result.total).toBe(56378)
    expect(result.records).toHaveLength(1)
    expect(result.records[0]).toEqual({
      code: '600519',
      name: '贵州茅台',
      exDate: '2026-06-20',
      pretaxBonusRmb: 300,
      assignProgress: '实施'
    })
  })

  it('mapResponse 对空数据返回空数组', () => {
    const result = eastmoneyMarketDividendEndpoint.mapResponse({ result: null } as never, { page: 1 })
    expect(result.records).toEqual([])
    expect(result.total).toBe(0)
  })
})
```

- [ ] **Step 2: 运行测试确认失败**

Run: `npx vitest run tests/main/dataSources/eastmoneyMarketEndpoints.test.ts`
Expected: FAIL（endpoint 未导出）

- [ ] **Step 3: 实现两个 endpoint**

在 `src/main/infrastructure/dataSources/registry/eastmoneyEndpoints.ts` 顶部 import 块追加：

```ts
import type {
  MarketClistInput,
  MarketClistOutput,
  MarketDividendInput,
  MarketDividendOutput,
  MarketQuoteRecord,
  MarketDividendRecord
} from '@main/infrastructure/dataSources/types/sourceTypes'
```

在 `eastmoneyHousingPriceIndexEndpoint` 之前追加：

```ts
// ====== Market-wide yield map endpoints ======

type EastmoneyClistResponse = {
  data?: {
    total?: number
    diff?: Array<{
      f2?: number | string
      f12?: string
      f13?: number
      f14?: string
      f100?: string | null
    }>
  }
}

type EastmoneyMarketDividendResponse = {
  result?: {
    count?: number
    data?: Array<{
      SECURITY_CODE?: string
      SECURITY_NAME_ABBR?: string
      EX_DIVIDEND_DATE?: string
      PRETAX_BONUS_RMB?: number
      ASSIGN_PROGRESS?: string
    }>
  }
}

const A_SHARE_LIST_FS =
  'm:0+t:6,m:0+t:80,m:1+t:2,m:1+t:23,m:0+t:81+s:2048'

export const eastmoneyMarketClistEndpoint: EndpointDefinition<
  MarketClistInput,
  EastmoneyClistResponse,
  MarketClistOutput
> = {
  id: 'eastmoney.market.clist',
  provider: 'eastmoney',
  capability: 'market.clist',
  parser: 'json',
  method: 'GET',
  timeoutMs: 10000,
  headers: { Referer: 'https://quote.eastmoney.com/' },
  buildUrl: ({ page, pageSize = 100 }) =>
    `https://push2.eastmoney.com/api/qt/clist/get?pn=${page}&pz=${pageSize}&po=1&np=1&fltt=2&invt=2&fid=f12` +
    `&fs=${A_SHARE_LIST_FS}&fields=f12,f13,f14,f2,f100`,
  mapResponse: (raw) => {
    const diff = raw.data?.diff ?? []
    const records: MarketQuoteRecord[] = diff.flatMap((item) => {
      const code = item.f12?.trim()
      if (!code) return []
      const rawPrice = item.f2
      const price =
        typeof rawPrice === 'number' && Number.isFinite(rawPrice) && rawPrice > 0
          ? rawPrice
          : undefined
      const industry = item.f100?.trim() || undefined
      return [
        {
          code,
          market: String(item.f13 ?? ''),
          name: item.f14?.trim() ?? '',
          price,
          industry
        }
      ]
    })
    return { records, total: raw.data?.total ?? records.length }
  }
}

export const eastmoneyMarketDividendEndpoint: EndpointDefinition<
  MarketDividendInput,
  EastmoneyMarketDividendResponse,
  MarketDividendOutput
> = {
  id: 'eastmoney.market.dividend',
  provider: 'eastmoney',
  capability: 'market.dividend',
  parser: 'json',
  method: 'GET',
  timeoutMs: 15000,
  buildUrl: ({ page, pageSize = 500 }) =>
    'https://datacenter-web.eastmoney.com/api/data/v1/get' +
    '?reportName=RPT_SHAREBONUS_DET&columns=ALL' +
    `&pageNumber=${page}&pageSize=${pageSize}` +
    '&sortColumns=EX_DIVIDEND_DATE&sortTypes=-1&source=WEB&client=WEB',
  mapResponse: (raw) => {
    const data = raw.result?.data ?? []
    const records: MarketDividendRecord[] = data.flatMap((item) => {
      const code = item.SECURITY_CODE?.trim()
      if (!code) return []
      const exDate = item.EX_DIVIDEND_DATE?.trim().slice(0, 10) || undefined
      return [
        {
          code,
          name: item.SECURITY_NAME_ABBR?.trim() || undefined,
          exDate,
          pretaxBonusRmb: item.PRETAX_BONUS_RMB,
          assignProgress: item.ASSIGN_PROGRESS?.trim() || undefined
        }
      ]
    })
    return { records, total: raw.result?.count ?? records.length }
  }
}
```

在文件末尾 `eastmoneyEndpoints` 数组追加两个元素：

```ts
export const eastmoneyEndpoints = [
  // ...现有元素...
  eastmoneyHousingPriceIndexEndpoint,
  eastmoneyMarketClistEndpoint,
  eastmoneyMarketDividendEndpoint
]
```

- [ ] **Step 4: 运行测试确认通过**

Run: `npx vitest run tests/main/dataSources/eastmoneyMarketEndpoints.test.ts`
Expected: PASS（5 个用例）

- [ ] **Step 5: typecheck + 提交**

Run: `npm run typecheck`
```bash
git add src/main/infrastructure/dataSources/registry/eastmoneyEndpoints.ts tests/main/dataSources/eastmoneyMarketEndpoints.test.ts
git commit -m "feat(yield-map): 东财全市场行情与分红分页 endpoint"
```

## Task B3: capabilityRouter 注册新 capability

**Files:**
- Modify: `src/main/infrastructure/dataSources/router/capabilityRouter.ts:57-62`

- [ ] **Step 1: 注册两个 capability**

在 `src/main/infrastructure/dataSources/router/capabilityRouter.ts` 的 `case 'housing.priceIndex':` 分支后追加：

```ts
      case 'market.clist':
      case 'market.dividend':
        return {
          primary: 'eastmoney',
          fallbacks: [],
          degradeMode: 'fallback'
        }
```

- [ ] **Step 2: typecheck + 提交**

Run: `npm run typecheck`
```bash
git add src/main/infrastructure/dataSources/router/capabilityRouter.ts
git commit -m "feat(yield-map): capabilityRouter 注册 market.clist/market.dividend"
```

## Task B4: YieldMapDataSource 适配器（并发分页抓取）

**Files:**
- Modify: `src/main/adapters/contracts.ts`
- Create: `src/main/adapters/eastmoney/eastmoneyYieldMapDataSource.ts`
- Test: `tests/main/adapters/eastmoneyYieldMapDataSource.test.ts`

**Interfaces:**
- Produces：
  - `export interface YieldMapDataSource`（contracts.ts）：`fetchAllQuotes(): Promise<MarketQuoteRecord[]>`、`fetchAllDividendEvents(): Promise<MarketDividendRecord[]>`
  - `export class EastmoneyYieldMapDataSource implements YieldMapDataSource`：并发 10 分页抓取，单页失败跳过

- [ ] **Step 1: 写失败测试（mock SourceGateway，断言分页与并发）**

创建 `tests/main/adapters/eastmoneyYieldMapDataSource.test.ts`：

```ts
import { beforeEach, describe, expect, it, vi } from 'vitest'
import type { MarketQuoteRecord, MarketDividendRecord } from '@main/infrastructure/dataSources/types/sourceTypes'

vi.mock('@main/infrastructure/dataSources/gateway/sourceGateway', () => ({
  getDefaultSourceGateway: () => mockGateway
}))

const mockGateway = {
  request: vi.fn()
}

const { EastmoneyYieldMapDataSource } = await import(
  '@main/adapters/eastmoney/eastmoneyYieldMapDataSource'
)

describe('EastmoneyYieldMapDataSource', () => {
  beforeEach(() => {
    mockGateway.request.mockReset()
  })

  it('fetchAllQuotes 按 total/100 分页抓取并合并，单页失败跳过', async () => {
    mockGateway.request
      .mockResolvedValueOnce({ data: { records: [{ code: '600519', market: '1', name: '贵州茅台', price: 1450, industry: '白酒' }], total: 400 } })
      .mockResolvedValueOnce({ data: { records: [{ code: '000001', market: '0', name: '平安银行', price: 10, industry: '银行' }], total: 400 } })
      .mockRejectedValueOnce(new Error('boom'))
      .mockResolvedValueOnce({ data: { records: [{ code: '000002', market: '0', name: '万科A', price: 8, industry: '地产' }], total: 400 } })

    const source = new EastmoneyYieldMapDataSource()
    const records = await source.fetchAllQuotes()
    expect(records).toHaveLength(3)
    expect(mockGateway.request).toHaveBeenCalledTimes(4)
    expect(mockGateway.request).toHaveBeenNthCalledWith(1, {
      capability: 'market.clist',
      input: { page: 1, pageSize: 100 },
      cacheKey: 'yield-map:clist:1',
      cacheTtlMs: 60 * 60 * 1000
    })
  })

  it('fetchAllQuotes 并发上限为 10', async () => {
    mockGateway.request.mockImplementation(async () => ({
      data: { records: [], total: 1000 }
    }))
    const source = new EastmoneyYieldMapDataSource()
    await source.fetchAllQuotes()
    expect(mockGateway.request).toHaveBeenCalledTimes(10)
  })

  it('fetchAllDividendEvents 分页抓取合并', async () => {
    mockGateway.request
      .mockResolvedValueOnce({ data: { records: [{ code: '600519', exDate: '2026-06-20', pretaxBonusRmb: 300 }], total: 600 } })
      .mockResolvedValueOnce({ data: { records: [{ code: '000001', exDate: '2026-05-01', pretaxBonusRmb: 20 }], total: 600 } })
      .mockResolvedValueOnce({ data: { records: [], total: 600 } })

    const source = new EastmoneyYieldMapDataSource()
    const records = await source.fetchAllDividendEvents()
    expect(records).toHaveLength(2)
    expect(mockGateway.request).toHaveBeenNthCalledWith(1, {
      capability: 'market.dividend',
      input: { page: 1, pageSize: 500 },
      cacheKey: 'yield-map:dividend:1',
      cacheTtlMs: 60 * 60 * 1000
    })
  })
})
```

- [ ] **Step 2: 运行测试确认失败**

Run: `npx vitest run tests/main/adapters/eastmoneyYieldMapDataSource.test.ts`
Expected: FAIL（模块不存在）

- [ ] **Step 3: 实现 contracts 接口与适配器**

在 `src/main/adapters/contracts.ts` 顶部 import 追加：

```ts
import type { MarketQuoteRecord, MarketDividendRecord } from '@main/infrastructure/dataSources/types/sourceTypes'
```

在文件末尾追加：

```ts
export interface YieldMapDataSource {
  fetchAllQuotes(): Promise<MarketQuoteRecord[]>
  fetchAllDividendEvents(): Promise<MarketDividendRecord[]>
}
```

创建 `src/main/adapters/eastmoney/eastmoneyYieldMapDataSource.ts`：

```ts
import type {
  MarketClistOutput,
  MarketDividendOutput,
  MarketQuoteRecord,
  MarketDividendRecord
} from '@main/infrastructure/dataSources/types/sourceTypes'
import type { YieldMapDataSource } from '@main/adapters/contracts'
import { getDefaultSourceGateway } from '@main/infrastructure/dataSources/gateway/sourceGateway'

const CONCURRENCY = 10
const CLIST_PAGE_SIZE = 100
const DIVIDEND_PAGE_SIZE = 500
const PAGE_CACHE_TTL_MS = 60 * 60 * 1000

async function fetchPages<TInput, TOutput>(
  capability: 'market.clist' | 'market.dividend',
  firstPage: TOutput,
  total: number,
  pageSize: number,
  inputFor: (page: number) => TInput,
  keyFor: (page: number) => string
): Promise<Array<TOutput extends { records: unknown } ? TOutput['records'] : never>> {
  const pageCount = Math.ceil(total / pageSize)
  const pagesToFetch = Math.max(0, pageCount - 1)
  const results: unknown[] = []

  for (let start = 0; start < pagesToFetch; start += CONCURRENCY) {
    const batch = Array.from(
      { length: Math.min(CONCURRENCY, pagesToFetch - start) },
      (_unused, index) => start + index + 2
    )
    const settled = await Promise.allSettled(
      batch.map((page) =>
        getDefaultSourceGateway().request<TInput, TOutput>({
          capability,
          input: inputFor(page),
          cacheKey: keyFor(page),
          cacheTtlMs: PAGE_CACHE_TTL_MS
        })
      )
    )
    for (const result of settled) {
      if (result.status === 'fulfilled') {
        results.push(result.value.records)
      }
    }
  }
  return results as Array<TOutput extends { records: unknown } ? TOutput['records'] : never>
}

export class EastmoneyYieldMapDataSource implements YieldMapDataSource {
  async fetchAllQuotes(): Promise<MarketQuoteRecord[]> {
    const first = await getDefaultSourceGateway().request<
      { page: number; pageSize: number },
      MarketClistOutput
    >({
      capability: 'market.clist',
      input: { page: 1, pageSize: CLIST_PAGE_SIZE },
      cacheKey: 'yield-map:clist:1',
      cacheTtlMs: PAGE_CACHE_TTL_MS
    })
    const rest = await fetchPages(
      'market.clist',
      first.data,
      first.data.total,
      CLIST_PAGE_SIZE,
      (page) => ({ page, pageSize: CLIST_PAGE_SIZE }),
      (page) => `yield-map:clist:${page}`
    )
    return [first.data.records, ...rest].flat()
  }

  async fetchAllDividendEvents(): Promise<MarketDividendRecord[]> {
    const first = await getDefaultSourceGateway().request<
      { page: number; pageSize: number },
      MarketDividendOutput
    >({
      capability: 'market.dividend',
      input: { page: 1, pageSize: DIVIDEND_PAGE_SIZE },
      cacheKey: 'yield-map:dividend:1',
      cacheTtlMs: PAGE_CACHE_TTL_MS
    })
    const rest = await fetchPages(
      'market.dividend',
      first.data,
      first.data.total,
      DIVIDEND_PAGE_SIZE,
      (page) => ({ page, pageSize: DIVIDEND_PAGE_SIZE }),
      (page) => `yield-map:dividend:${page}`
    )
    return [first.data.records, ...rest].flat()
  }
}
```

> 注：mock 测试中断言 `request` 被调用 `10` 次对应 `total=1000` 的 10 页（第 2~11 页），实现中 batch 并发上限 10 满足。

- [ ] **Step 4: 运行测试确认通过**

Run: `npx vitest run tests/main/adapters/eastmoneyYieldMapDataSource.test.ts`
Expected: PASS（3 个用例）

- [ ] **Step 5: typecheck + 提交**

Run: `npm run typecheck`
```bash
git add src/main/adapters/contracts.ts src/main/adapters/eastmoney/eastmoneyYieldMapDataSource.ts tests/main/adapters/eastmoneyYieldMapDataSource.test.ts
git commit -m "feat(yield-map): 全市场行情与分红并发分页适配器"
```

---

# Phase C：持久化 + useCase + IPC/HTTP 链路

## Task C1: SQLite yield_map_snapshots 迁移

**Files:**
- Modify: `src/main/infrastructure/db/sqlite.ts:228-262`
- Test: `tests/main/infrastructure/dbMigration.test.ts`

**Interfaces:**
- Produces：`function migrateYieldMapSnapshots(db: DatabaseSync)` 在 `initializeSchema` 中调用；表结构：

```sql
CREATE TABLE IF NOT EXISTS yield_map_snapshots (
  asset_key   TEXT PRIMARY KEY,
  symbol      TEXT NOT NULL,
  name        TEXT NOT NULL,
  industry    TEXT NOT NULL,
  price       REAL,
  yield_ttm   REAL NOT NULL,
  total_dps_12m REAL,
  fetched_at  TEXT NOT NULL
)
```

- [ ] **Step 1: 追加失败测试**

在 `tests/main/infrastructure/dbMigration.test.ts` 追加用例：

```ts
import { migrateYieldMapSnapshots } from '@main/infrastructure/db/sqlite'

describe('yield_map_snapshots migration', () => {
  it('创建 yield_map_snapshots 表并支持读写', () => {
    migrateYieldMapSnapshots(db)
    db.prepare(`
      INSERT INTO yield_map_snapshots (asset_key, symbol, name, industry, price, yield_ttm, total_dps_12m, fetched_at)
      VALUES ('STOCK:A_SHARE:600519', '600519', '贵州茅台', '白酒', 1450, 0.0207, 30, '2026-08-03T00:00:00.000Z')
    `).run()
    const row = db.prepare('SELECT * FROM yield_map_snapshots WHERE asset_key = ?').get('STOCK:A_SHARE:600519')
    expect(row).toMatchObject({
      symbol: '600519',
      industry: '白酒',
      yield_ttm: 0.0207
    })
  })
})
```

（`db` 为文件已有 beforeEach 创建的 `DatabaseSync(':memory:')` 实例；若该文件无既有 `db` 变量，按既有 describe 的建库方式建一个）

- [ ] **Step 2: 运行测试确认失败**

Run: `npx vitest run tests/main/infrastructure/dbMigration.test.ts`
Expected: FAIL（migrateYieldMapSnapshots 未导出）

- [ ] **Step 3: 实现迁移函数**

在 `src/main/infrastructure/db/sqlite.ts` 追加：

```ts
export function migrateYieldMapSnapshots(db: DatabaseSync) {
  db.exec(`
    CREATE TABLE IF NOT EXISTS yield_map_snapshots (
      asset_key     TEXT PRIMARY KEY,
      symbol        TEXT NOT NULL,
      name          TEXT NOT NULL,
      industry      TEXT NOT NULL,
      price         REAL,
      yield_ttm     REAL NOT NULL,
      total_dps_12m REAL,
      fetched_at    TEXT NOT NULL
    );
    CREATE INDEX IF NOT EXISTS idx_yield_map_snapshots_industry
      ON yield_map_snapshots(industry);
  `)
}
```

在 `initializeSchema` 中 `migrateDividendFiscalYearNotNull(db)` 之后追加：

```ts
  migrateYieldMapSnapshots(db)
```

- [ ] **Step 4: 运行测试确认通过**

Run: `npx vitest run tests/main/infrastructure/dbMigration.test.ts`
Expected: PASS

- [ ] **Step 5: typecheck + 提交**

Run: `npm run typecheck`
```bash
git add src/main/infrastructure/db/sqlite.ts tests/main/infrastructure/dbMigration.test.ts
git commit -m "feat(yield-map): yield_map_snapshots 表迁移"
```

## Task C2: YieldMapRepository（本地快照读写）

**Files:**
- Create: `src/main/repositories/yieldMapRepository.ts`
- Test: `tests/main/repositories/yieldMapRepository.test.ts`

**Interfaces:**
- Produces：
  - `export class YieldMapRepository`
  - `replaceAll(entries: YieldMapStockEntry[]): void`（清空并全量写入，事务）
  - `getAll(): YieldMapStockEntry[]`
  - `getFetchedAt(): string | null`（`MAX(fetched_at)`）
  - 测试模式：`vi.mock('@main/infrastructure/db/sqlite')` + `DatabaseSync(':memory:')`（仿 `valuationCacheRepository.test.ts` 既有模式）

- [ ] **Step 1: 写失败测试**

创建 `tests/main/repositories/yieldMapRepository.test.ts`：

```ts
import { mkdtempSync, rmSync } from 'node:fs'
import { tmpdir } from 'node:os'
import { join } from 'node:path'
import { DatabaseSync } from 'node:sqlite'
import { beforeEach, afterEach, describe, expect, it, vi } from 'vitest'

const tempDir = mkdtempSync(join(tmpdir(), 'yield-map-repo-'))
vi.mock('@main/infrastructure/db/sqlite', () => ({
  getDatabase: () => db,
  migrateYieldMapSnapshots: () => {}
}))

let db: DatabaseSync
const { YieldMapRepository } = await import('@main/repositories/yieldMapRepository')

const SAMPLE = [
  {
    assetKey: 'STOCK:A_SHARE:600519',
    symbol: '600519',
    name: '贵州茅台',
    industry: '白酒',
    price: 1450,
    yieldTtm: 0.0207,
    totalDps12m: 30
  },
  {
    assetKey: 'STOCK:A_SHARE:000001',
    symbol: '000001',
    name: '平安银行',
    industry: '银行',
    price: 10,
    yieldTtm: 0.05,
    totalDps12m: 0.5
  }
]

describe('YieldMapRepository', () => {
  beforeEach(() => {
    db = new DatabaseSync(join(tempDir, `db-${Date.now()}.sqlite`))
    db.exec(`
      CREATE TABLE IF NOT EXISTS yield_map_snapshots (
        asset_key     TEXT PRIMARY KEY,
        symbol        TEXT NOT NULL,
        name          TEXT NOT NULL,
        industry      TEXT NOT NULL,
        price         REAL,
        yield_ttm     REAL NOT NULL,
        total_dps_12m REAL,
        fetched_at    TEXT NOT NULL
      );
    `)
  })

  afterEach(() => {
    db.close()
    rmSync(tempDir, { recursive: true, force: true })
  })

  it('replaceAll 全量覆盖并记录 fetched_at', () => {
    const repo = new YieldMapRepository()
    repo.replaceAll(SAMPLE)
    const rows = repo.getAll()
    expect(rows).toHaveLength(2)
    expect(rows[0].symbol).toBe('600519')
    expect(repo.getFetchedAt()).toBeTruthy()
  })

  it('replaceAll 再次调用清空旧数据', () => {
    const repo = new YieldMapRepository()
    repo.replaceAll(SAMPLE)
    repo.replaceAll([SAMPLE[0]])
    expect(repo.getAll()).toHaveLength(1)
  })

  it('空库 getAll 返回空数组、getFetchedAt 返回 null', () => {
    const repo = new YieldMapRepository()
    expect(repo.getAll()).toEqual([])
    expect(repo.getFetchedAt()).toBeNull()
  })
})
```

- [ ] **Step 2: 运行测试确认失败**

Run: `npx vitest run tests/main/repositories/yieldMapRepository.test.ts`
Expected: FAIL（模块不存在）

- [ ] **Step 3: 实现 YieldMapRepository**

创建 `src/main/repositories/yieldMapRepository.ts`：

```ts
import { getDatabase } from '@main/infrastructure/db/sqlite'
import type { YieldMapStockEntry } from '@main/domain/services/yieldMapService'

type YieldMapRow = {
  asset_key: string
  symbol: string
  name: string
  industry: string
  price: number | null
  yield_ttm: number
  total_dps_12m: number | null
  fetched_at: string
}

function toEntry(row: YieldMapRow): YieldMapStockEntry {
  return {
    assetKey: row.asset_key,
    symbol: row.symbol,
    name: row.name,
    industry: row.industry,
    price: row.price ?? undefined,
    yieldTtm: row.yield_ttm,
    totalDps12m: row.total_dps_12m ?? undefined
  }
}

export class YieldMapRepository {
  replaceAll(entries: YieldMapStockEntry[]): void {
    const db = getDatabase()
    const now = new Date().toISOString()
    db.exec('BEGIN')
    try {
      db.prepare('DELETE FROM yield_map_snapshots').run()
      const insert = db.prepare(`
        INSERT INTO yield_map_snapshots
          (asset_key, symbol, name, industry, price, yield_ttm, total_dps_12m, fetched_at)
        VALUES (?, ?, ?, ?, ?, ?, ?, ?)
      `)
      for (const entry of entries) {
        insert.run(
          entry.assetKey,
          entry.symbol,
          entry.name,
          entry.industry,
          entry.price ?? null,
          entry.yieldTtm,
          entry.totalDps12m ?? null,
          now
        )
      }
      db.exec('COMMIT')
    } catch (error) {
      db.exec('ROLLBACK')
      throw error
    }
  }

  getAll(): YieldMapStockEntry[] {
    const rows = getDatabase()
      .prepare('SELECT * FROM yield_map_snapshots')
      .all() as unknown as YieldMapRow[]
    return rows.map(toEntry)
  }

  getFetchedAt(): string | null {
    const row = getDatabase()
      .prepare('SELECT MAX(fetched_at) AS latest FROM yield_map_snapshots')
      .get() as { latest: string | null }
    return row.latest ?? null
  }
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `npx vitest run tests/main/repositories/yieldMapRepository.test.ts`
Expected: PASS（3 个用例）

- [ ] **Step 5: typecheck + 提交**

Run: `npm run typecheck`
```bash
git add src/main/repositories/yieldMapRepository.ts tests/main/repositories/yieldMapRepository.test.ts
git commit -m "feat(yield-map): 本地快照仓储（全量覆盖/读取/时间戳）"
```

## Task C3: shared 契约 yieldMap 命名空间 + useCase

**Files:**
- Modify: `shared/contracts/api.ts`
- Create: `src/main/application/useCases/getMarketYieldMap.ts`
- Test: `tests/main/useCases/getMarketYieldMap.test.ts`

**Interfaces:**
- Produces：
  - `export type YieldMapStockDto = { assetKey: string; symbol: string; name: string; industry: string; price?: number; yieldTtm: number }`
  - `export type YieldMapIndustryDto = { industry: string; medianYield: number; avgYield: number; stockCount: number }`
  - `export type MarketYieldMapDto = { industries: YieldMapIndustryDto[]; stocks: YieldMapStockDto[]; fetchedAt?: string; partial: boolean; stockCount: number }`
  - `DividendMonitorApi` 增加 `yieldMap: { get(): Promise<MarketYieldMapDto>; refresh(): Promise<MarketYieldMapDto> }`
  - `export async function getMarketYieldMap(): Promise<MarketYieldMapDto>`：缓存新鲜（`fetchedAt` 距今 < 24h）直接读库；否则抓取→聚合→`replaceAll`→返回
  - `export async function refreshMarketYieldMap(): Promise<MarketYieldMapDto>`：强制抓取聚合入库

- [ ] **Step 1: 追加失败测试**

创建 `tests/main/useCases/getMarketYieldMap.test.ts`：

```ts
import { describe, expect, it, vi } from 'vitest'
import type { YieldMapStockEntry } from '@main/domain/services/yieldMapService'

const mockRepo = {
  replaceAll: vi.fn(),
  getAll: vi.fn(),
  getFetchedAt: vi.fn()
}

const mockSource = {
  fetchAllQuotes: vi.fn(),
  fetchAllDividendEvents: vi.fn()
}

vi.mock('@main/repositories/yieldMapRepository', () => ({ YieldMapRepository: class { replaceAll = mockRepo.replaceAll; getAll = mockRepo.getAll; getFetchedAt = mockRepo.getFetchedAt } }))
vi.mock('@main/adapters/eastmoney/eastmoneyYieldMapDataSource', () => ({ EastmoneyYieldMapDataSource: class { fetchAllQuotes = mockSource.fetchAllQuotes; fetchAllDividendEvents = mockSource.fetchAllDividendEvents } }))

const { getMarketYieldMap, refreshMarketYieldMap } = await import('@main/application/useCases/getMarketYieldMap')

const STALE_TS = new Date(Date.now() - 25 * 60 * 60 * 1000).toISOString()

describe('getMarketYieldMap', () => {
  it('缓存 24h 内直接读库不抓取', async () => {
    mockRepo.getFetchedAt.mockReturnValue(new Date().toISOString())
    mockRepo.getAll.mockReturnValue([
      { assetKey: 'STOCK:A_SHARE:600519', symbol: '600519', name: '贵州茅台', industry: '白酒', price: 1450, yieldTtm: 0.0207 }
    ] as YieldMapStockEntry[])
    const result = await getMarketYieldMap()
    expect(result.stockCount).toBe(1)
    expect(mockSource.fetchAllQuotes).not.toHaveBeenCalled()
  })

  it('缓存过期时抓取聚合入库', async () => {
    mockRepo.getFetchedAt.mockReturnValue(STALE_TS)
    mockRepo.getAll.mockReturnValue([])
    mockSource.fetchAllQuotes.mockResolvedValue([
      { code: '600519', market: '1', name: '贵州茅台', price: 1450, industry: '白酒' }
    ])
    mockSource.fetchAllDividendEvents.mockResolvedValue([
      { code: '600519', exDate: '2026-06-20', pretaxBonusRmb: 300 }
    ])
    const result = await getMarketYieldMap()
    expect(result.stockCount).toBe(1)
    expect(result.stocks[0].yieldTtm).toBeCloseTo(30 / 1450, 6)
    expect(result.industries[0].industry).toBe('白酒')
    expect(mockRepo.replaceAll).toHaveBeenCalledTimes(1)
    expect(result.fetchedAt).toBeTruthy()
  })

  it('refresh 强制抓取', async () => {
    mockSource.fetchAllQuotes.mockResolvedValue([])
    mockSource.fetchAllDividendEvents.mockResolvedValue([])
    const result = await refreshMarketYieldMap()
    expect(mockSource.fetchAllQuotes).toHaveBeenCalledTimes(1)
    expect(result.stockCount).toBe(0)
  })
})
```

- [ ] **Step 2: 运行测试确认失败**

Run: `npx vitest run tests/main/useCases/getMarketYieldMap.test.ts`
Expected: FAIL（模块不存在）

- [ ] **Step 3: 实现 shared 契约**

在 `shared/contracts/api.ts` 追加类型（放在 `DividendForecastDto` 定义之后）：

```ts
export type YieldMapStockDto = {
  assetKey: string
  symbol: string
  name: string
  industry: string
  price?: number
  yieldTtm: number
}

export type YieldMapIndustryDto = {
  industry: string
  medianYield: number
  avgYield: number
  stockCount: number
}

export type MarketYieldMapDto = {
  industries: YieldMapIndustryDto[]
  stocks: YieldMapStockDto[]
  fetchedAt?: string
  partial: boolean
  stockCount: number
}
```

在 `DividendMonitorApi` 的 `dividend: { ... }` 命名空间后追加：

```ts
  yieldMap: {
    get(): Promise<MarketYieldMapDto>
    refresh(): Promise<MarketYieldMapDto>
  }
```

- [ ] **Step 4: 实现 useCase**

创建 `src/main/application/useCases/getMarketYieldMap.ts`：

```ts
import type { MarketYieldMapDto } from '@shared/contracts/api'
import { buildYieldMap, buildIndustryYieldMap, type YieldMapStockEntry } from '@main/domain/services/yieldMapService'
import { YieldMapRepository } from '@main/repositories/yieldMapRepository'
import { EastmoneyYieldMapDataSource } from '@main/adapters/eastmoney/eastmoneyYieldMapDataSource'

const SNAPSHOT_TTL_MS = 24 * 60 * 60 * 1000

const repository = new YieldMapRepository()
const dataSource = new EastmoneyYieldMapDataSource()

function toDto(entries: YieldMapStockEntry[], fetchedAt: string | null): MarketYieldMapDto {
  return {
    industries: buildIndustryYieldMap(entries),
    stocks: entries.map((entry) => ({
      assetKey: entry.assetKey,
      symbol: entry.symbol,
      name: entry.name,
      industry: entry.industry,
      price: entry.price,
      yieldTtm: entry.yieldTtm
    })),
    fetchedAt: fetchedAt ?? undefined,
    partial: false,
    stockCount: entries.length
  }
}

export async function getMarketYieldMap(): Promise<MarketYieldMapDto> {
  const fetchedAt = repository.getFetchedAt()
  if (fetchedAt) {
    const age = Date.now() - new Date(fetchedAt).getTime()
    if (age >= 0 && age < SNAPSHOT_TTL_MS) {
      const entries = repository.getAll()
      if (entries.length > 0) {
        return toDto(entries, fetchedAt)
      }
    }
  }
  return refreshMarketYieldMap()
}

export async function refreshMarketYieldMap(): Promise<MarketYieldMapDto> {
  const [quotes, events] = await Promise.all([
    dataSource.fetchAllQuotes(),
    dataSource.fetchAllDividendEvents()
  ])
  const entries = buildYieldMap(quotes, events)
  repository.replaceAll(entries)
  const fetchedAt = repository.getFetchedAt()
  return toDto(entries, fetchedAt)
}
```

- [ ] **Step 5: 运行测试确认通过**

Run: `npx vitest run tests/main/useCases/getMarketYieldMap.test.ts`
Expected: PASS（3 个用例）

- [ ] **Step 6: typecheck + 提交**

Run: `npm run typecheck`
```bash
git add shared/contracts/api.ts src/main/application/useCases/getMarketYieldMap.ts tests/main/useCases/getMarketYieldMap.test.ts
git commit -m "feat(yield-map): 快照缓存 + 抓取聚合 useCase 与 shared 契约"
```

## Task C4: IPC 通道 + HTTP 路由 + preload

**Files:**
- Create: `src/main/ipc/channels/yieldMapChannels.ts`
- Modify: `src/main/ipc/channels/index.ts`（注册通道）
- Modify: `src/main/http/routes/index.ts` 或 `src/main/http/server.ts`（注册路由，仿 dividend 的注册点）
- Create: `src/main/http/routes/yieldMapRoutes.ts`
- Modify: `src/preload/index.ts:117-121`

**Interfaces:**
- Produces：
  - IPC：`yield-map:get`、`yield-map:refresh`
  - HTTP：`GET/POST /api/yield-map`（get）、`POST /api/yield-map/refresh`（refresh）
  - preload：`yieldMap: { get, refresh }`

- [ ] **Step 1: 创建 IPC 通道**

创建 `src/main/ipc/channels/yieldMapChannels.ts`：

```ts
import { ipcMain } from 'electron'
import { getMarketYieldMap, refreshMarketYieldMap } from '@main/application/useCases/getMarketYieldMap'

export function registerYieldMapChannels() {
  ipcMain.handle('yield-map:get', async () => {
    return getMarketYieldMap()
  })
  ipcMain.handle('yield-map:refresh', async () => {
    return refreshMarketYieldMap()
  })
}
```

在 `src/main/ipc/channels/index.ts` 导入并调用 `registerYieldMapChannels()`（仿 `registerDividendChannels` 的注册位置）。

- [ ] **Step 2: 创建 HTTP 路由**

创建 `src/main/http/routes/yieldMapRoutes.ts`：

```ts
import type { ServerResponse } from 'node:http'
import { getMarketYieldMap, refreshMarketYieldMap } from '@main/application/useCases/getMarketYieldMap'
import { sendJson } from '@main/http/httpErrors'

type RouteContext = {
  pathname: string
  method: string
  body: unknown
  response: ServerResponse
}

export async function handleYieldMapRoute({ pathname, method, response }: RouteContext): Promise<boolean> {
  if (pathname === '/api/yield-map' && (method === 'GET' || method === 'POST')) {
    const result = await getMarketYieldMap()
    sendJson(response, 200, result)
    return true
  }

  if (pathname === '/api/yield-map/refresh' && (method === 'GET' || method === 'POST')) {
    const result = await refreshMarketYieldMap()
    sendJson(response, 200, result)
    return true
  }

  return false
}
```

在 `src/main/http/server.ts`（或既有路由分发处，仿 `handleDividendRoute` 的挂载点）挂载 `handleYieldMapRoute`。

- [ ] **Step 3: preload 注入**

在 `src/preload/index.ts` 的 `dividend: { ... }` 后追加：

```ts
  yieldMap: {
    get: () => ipcRenderer.invoke('yield-map:get'),
    refresh: () => ipcRenderer.invoke('yield-map:refresh')
  }
```

- [ ] **Step 4: typecheck + 提交**

Run: `npm run typecheck`
```bash
git add src/main/ipc/channels/yieldMapChannels.ts src/main/ipc/channels/index.ts src/main/http/routes/yieldMapRoutes.ts src/main/http/server.ts src/preload/index.ts
git commit -m "feat(yield-map): IPC 通道、HTTP 路由与 preload 暴露"
```

---

# Phase D：渲染进程

## Task D1: renderer runtime 链路 + mock

**Files:**
- Modify: `src/renderer/src/services/desktopApi.ts:129-146`
- Modify: `src/renderer/src/services/browserHttpRuntimeApi.ts`
- Modify: `src/renderer/src/services/browserRuntimeApi.ts`
- Create: `src/renderer/src/services/yieldMapApi.ts`

**Interfaces:**
- Produces：`export const yieldMapApi = getYieldMapDesktopApi()`（desktopApi 模式，仿 `dividendApi.ts`）；mock 返回固定样例（2 个行业、3 只股票）

- [ ] **Step 1: desktopApi 增加 getYieldMapDesktopApi**

在 `src/renderer/src/services/desktopApi.ts`（仿 `getDividendDesktopApi`，文件已有 `DividendMonitorApi['dividend']` 的取法）：

```ts
export function getYieldMapDesktopApi(): DividendMonitorApi['yieldMap'] {
  if (!api.yieldMap) {
    throw new Error('Runtime API is missing the yieldMap namespace.')
  }
  return api.yieldMap
}
```

创建 `src/renderer/src/services/yieldMapApi.ts`：

```ts
import { getYieldMapDesktopApi } from '@renderer/services/desktopApi'

export const yieldMapApi = getYieldMapDesktopApi()
```

- [ ] **Step 2: browserRuntimeApi mock**

在 `src/renderer/src/services/browserRuntimeApi.ts` 的 `dividend: { ... }` 后追加：

```ts
  yieldMap: {
    async get() {
      return {
        industries: [
          { industry: '银行', medianYield: 0.048, avgYield: 0.052, stockCount: 42 },
          { industry: '煤炭', medianYield: 0.065, avgYield: 0.071, stockCount: 33 }
        ],
        stocks: [
          { assetKey: 'STOCK:A_SHARE:601398', symbol: '601398', name: '工商银行', industry: '银行', price: 5.2, yieldTtm: 0.058 },
          { assetKey: 'STOCK:A_SHARE:601088', symbol: '601088', name: '中国神华', industry: '煤炭', price: 38.5, yieldTtm: 0.072 },
          { assetKey: 'STOCK:A_SHARE:600519', symbol: '600519', name: '贵州茅台', industry: '白酒', price: 1450, yieldTtm: 0.021 }
        ],
        fetchedAt: new Date().toISOString(),
        partial: false,
        stockCount: 3
      }
    },
    async refresh() {
      return this.get()
    }
  }
```

- [ ] **Step 3: browserHttpRuntimeApi 桥接**

在 `src/renderer/src/services/browserHttpRuntimeApi.ts` 的 `dividend: { ... }` 后追加：

```ts
  yieldMap: {
    get: () => getJson('/api/yield-map'),
    refresh: () => postJson('/api/yield-map/refresh', {})
  }
```

（`getJson`/`postJson` 为该文件既有辅助函数，返回 `Promise<unknown>` 处用既有类型断言方式处理）

- [ ] **Step 4: typecheck + 提交**

Run: `npm run typecheck`
```bash
git add src/renderer/src/services/desktopApi.ts src/renderer/src/services/browserRuntimeApi.ts src/renderer/src/services/browserHttpRuntimeApi.ts src/renderer/src/services/yieldMapApi.ts
git commit -m "feat(yield-map): renderer runtime 链路与 mock 数据"
```

## Task D2: YieldMapPage（Treemap + 维度 + 跳转）

**Files:**
- Create: `src/renderer/src/pages/YieldMapPage.tsx`
- Create: `src/renderer/src/components/yield-map/YieldMapTreemap.tsx`
- Modify: `src/renderer/src/router/AppRouter.tsx`（路由 `/yield-map`）
- Modify: `src/renderer/src/layouts/AppShell.tsx`（导航项）

**Interfaces:**
- Produces：`YieldMapPage` 使用 `useFetch` + `PageState`；主图 `YieldMapTreemap`（ECharts treemap 两层：行业 → 股票，visualMap 分段着色，点击股票跳 `/stock/600519` 详情）

- [ ] **Step 1: 创建 YieldMapTreemap 组件**

创建 `src/renderer/src/components/yield-map/YieldMapTreemap.tsx`：

```tsx
import { useRef, useEffect } from 'react'
import * as echarts from 'echarts'
import type { YieldMapIndustryDto, YieldMapStockDto } from '@shared/contracts/api'

type Props = {
  industries: YieldMapIndustryDto[]
  stocks: YieldMapStockDto[]
  onSelectStock: (symbol: string) => void
}

function yieldColor(yieldTtm: number): string {
  if (yieldTtm <= 0) return '#8b949e'
  if (yieldTtm < 0.02) return '#6ba3d6'
  if (yieldTtm < 0.035) return '#2ea86b'
  if (yieldTtm < 0.05) return '#f0b429'
  if (yieldTtm < 0.07) return '#e8773e'
  return '#b31b25'
}

export function YieldMapTreemap({ industries, stocks, onSelectStock }: Props) {
  const containerRef = useRef<HTMLDivElement>(null)
  const chartRef = useRef<echarts.ECharts | null>(null)

  useEffect(() => {
    if (!containerRef.current) return
    const chart = echarts.init(containerRef.current)
    chartRef.current = chart

    const byIndustry = new Map<string, YieldMapStockDto[]>()
    for (const stock of stocks) {
      const list = byIndustry.get(stock.industry) ?? []
      list.push(stock)
      byIndustry.set(stock.industry, list)
    }

    chart.setOption({
      tooltip: {
        formatter: (params: { name: string; data: { yieldTtm?: number; symbol?: string; medianYield?: number } }) =>
          `${params.name}<br/>股息率(TTM): ${params.data.yieldTtm != null ? (params.data.yieldTtm * 100).toFixed(2) + '%' : '--'}`
      },
      series: [
        {
          type: 'treemap',
          roam: false,
          nodeClick: false,
          breadcrumb: { show: true, height: 24 },
          label: { show: true, formatter: '{b}' },
          upperLabel: { show: true, height: 24, formatter: '{b}' },
          itemStyle: { borderColor: '#1a1f2b', borderWidth: 1, gapWidth: 2 },
          levels: [
            {
              itemStyle: { borderColor: '#0f141e', borderWidth: 3, gapWidth: 3 }
            },
            {
              itemStyle: { gapWidth: 1 },
              colorSaturation: [0.35, 0.5],
              itemStyle: { borderColor: '#1a1f2b', borderWidth: 1, gapWidth: 1 }
            }
          ],
          data: industries.map((industry) => ({
            name: industry.industry,
            value: industry.stockCount,
            itemStyle: { color: yieldColor(industry.medianYield) },
            children: (byIndustry.get(industry.industry) ?? []).map((stock) => ({
              name: `${stock.name} (${stock.symbol})`,
              value: 1,
              symbol: stock.symbol,
              yieldTtm: stock.yieldTtm,
              itemStyle: { color: yieldColor(stock.yieldTtm) }
            }))
          }))
        }
      ]
    })

    chart.on('click', (params) => {
      const symbol = (params.data as { symbol?: string }).symbol
      if (symbol) {
        onSelectStock(symbol)
      }
    })

    return () => {
      chart.dispose()
      chartRef.current = null
    }
  }, [industries, stocks, onSelectStock])

  return <div ref={containerRef} style={{ width: '100%', height: 560 }} />
}
```

> 注：图例以图例条形式放在页面（下方渲染 `YieldMapLegend` 简化版：5 段色块 + 标签），treemap 内不重复。

- [ ] **Step 2: 创建 YieldMapPage**

创建 `src/renderer/src/pages/YieldMapPage.tsx`：

```tsx
import { Button, Card, message as antdMessage } from 'antd'
import { useCallback } from 'react'
import { useNavigate } from 'react-router-dom'
import { PageState } from '@renderer/components/app/PageState'
import { YieldMapTreemap } from '@renderer/components/yield-map/YieldMapTreemap'
import { useFetch } from '@renderer/hooks/useFetch'
import { yieldMapApi } from '@renderer/services/yieldMapApi'

const LEGEND = [
  { color: '#b31b25', label: '≥7%' },
  { color: '#e8773e', label: '5%~7%' },
  { color: '#f0b429', label: '3.5%~5%' },
  { color: '#2ea86b', label: '2%~3.5%' },
  { color: '#6ba3d6', label: '<2%' },
  { color: '#8b949e', label: '无分红' }
]

export function YieldMapPage() {
  const navigate = useNavigate()
  const { data, loading, error, reload } = useFetch(
    () => yieldMapApi.get(),
    [],
    { rethrow: false }
  )

  const handleRefresh = useCallback(async () => {
    antdMessage.loading({ content: '正在抓取全市场数据…', key: 'yield-map-refresh' })
    try {
      await yieldMapApi.refresh()
      await reload()
      antdMessage.success({ content: '股息率地图已更新', key: 'yield-map-refresh' })
    } catch (err) {
      antdMessage.error({ content: err instanceof Error ? err.message : '刷新失败', key: 'yield-map-refresh' })
    }
  }, [reload])

  return (
    <PageState loading={loading} error={error} skeletonRows={12}>
      <div style={{ padding: 24 }}>
        <div style={{ display: 'flex', alignItems: 'center', justifyContent: 'space-between', marginBottom: 16 }}>
          <h2 style={{ margin: 0, fontSize: 20, fontWeight: 600 }}>股息率地图</h2>
          <Button onClick={() => void handleRefresh()}>刷新数据</Button>
        </div>
        {data && (
          <>
            <Card size="small" style={{ marginBottom: 16 }}>
              <div style={{ display: 'flex', gap: 16, alignItems: 'center', flexWrap: 'wrap' }}>
                {LEGEND.map((item) => (
                  <span key={item.label} style={{ display: 'inline-flex', alignItems: 'center', gap: 6 }}>
                    <span style={{ width: 14, height: 14, background: item.color, borderRadius: 3, display: 'inline-block' }} />
                    {item.label}
                  </span>
                ))}
                <span style={{ marginLeft: 'auto', color: '#66707a', fontSize: 12 }}>
                  {data.stockCount > 0 ? `全市场 ${data.stockCount} 只 · 更新于 ${new Date(data.fetchedAt ?? '').toLocaleString('zh-CN')}` : '暂无数据'}
                </span>
              </div>
            </Card>
            <Card size="small" title="按行业分布（块大小=样本数，颜色=中位数股息率）">
              <YieldMapTreemap
                industries={data.industries}
                stocks={data.stocks}
                onSelectStock={(symbol) => navigate(`/stock/${symbol}`)}
              />
            </Card>
          </>
        )}
      </div>
    </PageState>
  )
}
```

- [ ] **Step 3: 路由与导航**

在 `src/renderer/src/router/AppRouter.tsx` 注册路由（仿 `/industry-analysis` 的注册方式）：

```tsx
import { YieldMapPage } from '@renderer/pages/YieldMapPage'
// ...
<Route path="/yield-map" element={<YieldMapPage />} />
```

在 `src/renderer/src/layouts/AppShell.tsx` 导航菜单加"股息率地图"项（仿"行业分析"项，跳 `/yield-map`）。

- [ ] **Step 4: typecheck + 提交**

Run: `npm run typecheck`
```bash
git add src/renderer/src/components/yield-map/YieldMapTreemap.tsx src/renderer/src/pages/YieldMapPage.tsx src/renderer/src/router/AppRouter.tsx src/renderer/src/layouts/AppShell.tsx
git commit -m "feat(yield-map): 股息率地图页面（Treemap 行业/股票 drill-down）"
```

---

# Phase E：Supabase 云端行业快照

## Task E1: Supabase industry_yield_snapshots 表 + RLS

**Files:**
- Supabase migration（通过 `supabase apply_migration` 或 SQL Editor）

- [ ] **Step 1: 建表迁移**

```sql
create table if not exists public.industry_yield_snapshots (
  id            uuid primary key default gen_random_uuid(),
  user_id       uuid not null references auth.users(id) on delete cascade,
  industry      text not null,
  snapshot_date date not null,
  median_yield  real not null,
  avg_yield     real not null,
  stock_count   integer not null,
  fetched_at    timestamptz not null default now(),
  unique (user_id, industry, snapshot_date)
);

alter table public.industry_yield_snapshots enable row level security;

create policy "industry snapshots select own"
  on public.industry_yield_snapshots for select
  using (auth.uid() = user_id);

create policy "industry snapshots insert own"
  on public.industry_yield_snapshots for insert
  with check (auth.uid() = user_id);

create policy "industry snapshots update own"
  on public.industry_yield_snapshots for update
  using (auth.uid() = user_id);

create policy "industry snapshots delete own"
  on public.industry_yield_snapshots for delete
  using (auth.uid() = user_id);
```

执行后运行 `supabase get_advisors`（security）确认无告警。

- [ ] **Step 2: 提交（Supabase 无 git 提交，记录于文档）**

在 `docs/yield-map-research.md` 的 §7 后续实施记录追加一行（或用独立 doc 记录迁移 SQL），并 commit：

```bash
git add docs/yield-map-research.md
git commit -m "docs(yield-map): 记录 Supabase industry_yield_snapshots 迁移"
```

## Task E2: SupabaseYieldMapRepository + repositoryFactory 接线

**Files:**
- Create: `src/main/repositories/supabaseYieldMapRepository.ts`
- Modify: `src/main/repositories/repositoryFactory.ts:88-100`
- Modify: `src/main/application/useCases/getMarketYieldMap.ts`

**Interfaces:**
- Produces：
  - `export class SupabaseYieldMapRepository`：`getLatestIndustries(): Promise<YieldMapIndustryDto[]>`、`upsertIndustries(industries: YieldMapIndustryDto[], snapshotDate: string): Promise<void>`
  - `repositoryFactory.getYieldMapRepository(mode)`：online → Supabase，offline → 本地（本地仓储直接 new）
  - `getMarketYieldMap` 在线模式下：读云端行业级快照 + 本地股票级（若股票级缺失则触发 refresh）

- [ ] **Step 1: 创建 Supabase 仓储**

创建 `src/main/repositories/supabaseYieldMapRepository.ts`：

```ts
import { supabaseClient } from '@main/infrastructure/supabase/client'
import type { YieldMapIndustryDto } from '@shared/contracts/api'

type IndustrySnapshotRow = {
  industry: string
  snapshot_date: string
  median_yield: number
  avg_yield: number
  stock_count: number
}

export class SupabaseYieldMapRepository {
  async getLatestIndustries(): Promise<YieldMapIndustryDto[]> {
    const { data, error } = await supabaseClient
      .from('industry_yield_snapshots')
      .select('industry, snapshot_date, median_yield, avg_yield, stock_count')
      .order('snapshot_date', { ascending: false })
      .limit(500)
    if (error) {
      throw new Error(`读取云端行业快照失败: ${error.message}`)
    }
    const rows = (data ?? []) as unknown as IndustrySnapshotRow[]
    const latestDate = rows[0]?.snapshot_date
    return rows
      .filter((row) => row.snapshot_date === latestDate)
      .map((row) => ({
        industry: row.industry,
        medianYield: row.median_yield,
        avgYield: row.avg_yield,
        stockCount: row.stock_count
      }))
  }

  async upsertIndustries(industries: YieldMapIndustryDto[], snapshotDate: string): Promise<void> {
    const rows = industries.map((item) => ({
      industry: item.industry,
      snapshot_date: snapshotDate,
      median_yield: item.medianYield,
      avg_yield: item.avgYield,
      stock_count: item.stockCount
    }))
    if (rows.length === 0) return
    const { error } = await supabaseClient
      .from('industry_yield_snapshots')
      .upsert(rows, { onConflict: 'user_id,industry,snapshot_date' })
    if (error) {
      throw new Error(`写入云端行业快照失败: ${error.message}`)
    }
  }
}
```

（`supabaseClient` 按 `src/main/infrastructure/supabase/client.ts` 既有导出名引用，若不同按实际导出调整）

- [ ] **Step 2: repositoryFactory 接线**

在 `src/main/repositories/repositoryFactory.ts` 追加（仿 `getDividendRepository`）：

```ts
import { SupabaseYieldMapRepository } from '@main/repositories/supabaseYieldMapRepository'
import { YieldMapRepository } from '@main/repositories/yieldMapRepository'

export function getYieldMapRepository(mode?: AppRuntimeMode) {
  const runtimeMode = mode ?? getRuntimeMode()
  if (runtimeMode === 'online') {
    return new SupabaseYieldMapRepository()
  }
  return new YieldMapRepository()
}
```

> 注：在线模式返回类型为联合（Supabase 仓储 + 本地仓储不同类），调用方按需用 `instanceof` 或定义统一接口 `IYieldMapSnapshotRepository`（getLatestIndustries/upsertIndustries + getAll/getFetchedAt/replaceAll）由两个类各自实现。

- [ ] **Step 3: useCase 在线接线**

修改 `src/main/application/useCases/getMarketYieldMap.ts`：在 `toDto` 组装后，若 `getRuntimeMode() === 'online'`，用 `getYieldMapRepository('online')` 上传行业快照；读取路径：在线且无本地股票数据时，尝试云端行业快照填充 `industries`（`stocks` 为空时页面提示"等待本地刷新"）。

- [ ] **Step 4: typecheck + 提交**

Run: `npm run typecheck`
```bash
git add src/main/repositories/supabaseYieldMapRepository.ts src/main/repositories/repositoryFactory.ts src/main/application/useCases/getMarketYieldMap.ts
git commit -m "feat(yield-map): Supabase 行业快照读写与在线模式接线"
```

---

# Phase F：最终验收

## Task F1: 全面回归 + 端到端验证

- [ ] **Step 1: 全量测试 + typecheck**

Run: `npm test`
Expected: 全部通过（含新增 20+ 用例）

Run: `npm run typecheck`
Expected: 零错误

- [ ] **Step 2: 桌面端手动验收（MCP）**

1. `npm run dev` 启动，进入 `/yield-map`：首屏触发抓取（~10 秒），显示行业 Treemap
2. 点击行业块 drill-down 到股票层，颜色与图例一致
3. 点击股票块跳转 `/stock/<symbol>` 详情页
4. 刷新按钮：二次进入页面秒开（命中 24h 快照）；点击"刷新数据"重新抓取并更新时间戳
5. 检查 dev 库 `yield_map_snapshots` 有 ~5,888 行、`fetched_at` 已更新

- [ ] **Step 3: 在线模式验收（MCP）**

1. 登录后进入 `/yield-map`：`industry_yield_snapshots` 出现当前用户快照行
2. 云端快照读取正常（换设备登录同一账号可见行业层）

- [ ] **Step 4: 提交**

```bash
git commit -m "docs(yield-map): 验收记录（桌面 + 在线模式）"
```

## 执行交接

计划已保存至 `docs/superpowers/plans/2026-08-03-yield-map.md`，两种执行方式：

1. **Subagent-Driven（推荐）**：每个 Task 派独立子代理执行，任务间审查
2. **Inline Execution**：本会话内按 executing-plans 批量执行，带检查点

选哪种？
